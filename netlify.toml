[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_CONVEX_URL = "https://joyous-deer-167.eu-west-1.convex.cloud"

# SPA redirect — alle Routen zur index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
